name: Amazonite Owner Auth Tests

on:
  push:
    paths:
      - 'api/auth/**'
      - 'api/admin/**'
      - 'api/analytics/**'
      - 'lib/internal-auth.mjs'
      - 'lib/owner-auth.mjs'
      - 'lib/product-store.mjs'
      - 'lib/analytics-store.mjs'
      - 'middleware.js'
      - 'owner-login.html'
      - 'owner-setup.html'
      - 'test/**'
      - 'package.json'
      - '.github/workflows/owner-auth-tests.yml'
  pull_request:
    paths:
      - 'api/auth/**'
      - 'api/admin/**'
      - 'api/analytics/**'
      - 'lib/internal-auth.mjs'
      - 'lib/owner-auth.mjs'
      - 'lib/product-store.mjs'
      - 'lib/analytics-store.mjs'
      - 'middleware.js'
      - 'owner-login.html'
      - 'owner-setup.html'
      - 'test/**'
      - 'package.json'
      - '.github/workflows/owner-auth-tests.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
      - name: Run owner authentication, middleware, storage and analytics tests
        run: node --test test/owner-auth.test.mjs test/middleware-auth.test.mjs test/internal-auth.test.mjs test/product-api-auth.test.mjs test/product-store.test.mjs test/analytics-store.test.mjs
